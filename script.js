//your JS code here. If required.
let currentUrl = document.URL;
let urlLength = currentUrl.length;

// Log the URL and its length to the console
console.log("The URL is: " + currentUrl);
console.log("The length of the URL is: " + urlLength);